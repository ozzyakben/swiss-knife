# deps
node_modules/
**/node_modules/

# next
.next/
out/
next-env.d.ts

# prisma / sqlite (local data — never commit)
*.db
*.db-journal
**/data/

# quick-capture image uploads (local data — never commit)
cockpit/uploads/
uploads/

# project packs — local/internal project data (e.g. LBMH = ECI IP); never commit
cockpit/projects/

# env & secrets
.env
.env.local
*.local

# os / editor
.DS_Store
*.log
.idea/
.vscode/

# build
dist/
build/

# test artifacts
playwright-report/
test-results/
.playwright-mcp/

# local screenshot / verification scratch — never commit root-level image binaries
/*.png
/*.jpg
/*.jpeg

# typescript incremental build info
*.tsbuildinfo
