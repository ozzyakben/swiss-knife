node_modules
.next
.git
# **/ matters: dockerignore globs are root-anchored, and the local-dev SQLite
# lives at prisma/dev.db — a bare *.db left it baked into the image layers.
**/*.db
**/*.db-journal
**/*.db-wal
**/*.db-shm
data
.env
.env.*
projects
uploads
test-results
playwright-report
e2e
*.tsbuildinfo
