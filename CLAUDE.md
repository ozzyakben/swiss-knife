# CLAUDE.md — Project context for Claude Code

This file orients any AI agent working in this repo. Read it before making changes.

## What this is

**Swiss Knife** — a locally-run, private "daily cockpit" web app powered by **local Gemma** (via Ollama). It's a personal productivity + AI-tools hub that runs entirely on the user's machine. No third-party logging; all data stays on-machine.

Target machines: Apple Silicon Mac (M5 / 48GB, the original) and Windows 11 (Docker Desktop + native Ollama, optional NVIDIA GPU). Built to be shareable with colleagues — `./swiss` (macOS/bash) and `.\swiss` → `swiss.ps1` (Windows) are the per-platform ops CLIs.

## Hard rules (do not violate)

1. **Ollama runs NATIVELY on the host OS, never in Docker.** macOS: Docker Desktop cannot pass the Apple GPU, so containerized Ollama is CPU-only (~5–6× slower). Windows: the native app uses CUDA directly and containerizing adds a WSL2 GPU dance for no benefit. Containers reach the host Ollama via `host.docker.internal`.
2. **Keep everything local.** No cloud LLM calls from the cockpit. The whole point is privacy + zero API cost for daily tasks.
3. **Respect the tiering.** Local Gemma 12B handles drafting, cleanup, summarizing, organizing, and RAG Q&A. It is NOT for heavy agentic/coding work — that stays with Claude/Cursor. Don't design features that demand more than a 12B can deliver.
4. **Don't commit secrets or local data.** `.env`, `*.db`, and `node_modules/` are gitignored. Keep it that way.

## Engine model

Two chat tiers, switchable live in **Settings → Model** (a picker that lists installed Ollama models via `/api/models`, plus presets and a custom-tag fallback). Tier metadata + RAM hints live in `src/lib/models.ts`.

- **Light (default) — `gemma4:e4b`** (Gemma "effective-4B"; ~4 GB RAM). The default everywhere (`src/lib/config.ts` / `src/lib/ollama.ts`, `docker-compose.yml`), picked for RAM headroom when Open WebUI runs alongside. Also `gemma4:e2b` (~2 GB) as a preset.
- **Quality — platform-specific.** macOS Apple Silicon: `gemma4:12b-mlx` (MLX/safetensors; ~10–14 GB RAM, 256K context). Windows/Linux: **`gemma4:12b` (GGUF)** — the MLX tag will not run there (presets carry both, `src/lib/models.ts`; memory consolidation's quality step honors `OLLAMA_QUALITY_MODEL`). One click away in the picker, or set `OLLAMA_MODEL`.

`scripts/pull-models.sh` (macOS/Linux, platform-aware quality tag) and `scripts/pull-models.ps1` (Windows) pull the tiers plus `embeddinggemma`, light tier first so a failed quality pull never strands a machine. The Settings row overrides env at runtime via `getEffectiveConfig()`.

**Vision (important, counter-intuitive):** `gemma4:e4b` is the **vision-capable** model; `gemma4:12b-mlx` has **NO vision** (it silently ignores images and hallucinates). And image input must go through Ollama's **native** `/api/chat` with an `images: [base64]` array — the OpenAI-compatible `/v1` `image_url` path fails for GGUF vision models ("Failed to load image"). So image requests use `chatWithImages` / `streamChatWithImages` (`src/lib/ollama.ts`) + `lib/vision.describeImage`, with model = `getEffectiveConfig().visionModel` (`OLLAMA_VISION_MODEL`, default `gemma4:e4b`). Regular text chat stays on `/v1`. Used by `api/vision` (streamed) and `api/capture` (one-shot).

**Runtime requirement (important):** Ollama must be the official **app** for the platform. macOS: `brew install --cask ollama-app`, NOT the Homebrew CLI formula (`brew install ollama`) — the formula's bottle ships without the `llama-server` runner, so GGUF models fail with "llama-server binary not found"; the app bundles all runners (GGUF + MLX); models live in `~/.ollama`. Windows: `winget install Ollama.Ollama` (or ollama.com/download/windows) — the installer always bundles the runners (no formula trap there); models live in `%USERPROFILE%\.ollama`.

## Architecture (3 layers)

- **Engine:** Ollama (native on the host) serving `gemma4:e4b` (default) + the platform quality tier (`12b-mlx` mac / `12b` win) + an embedding model. OpenAI-compatible API at `http://localhost:11434/v1`.
- **Deep-work surface:** Open WebUI (Docker, off-the-shelf) — chat, document RAG, multimodal, prompt library. Configured, not coded.
- **Cockpit (the part we build):** Next.js 15 app (this repo's `cockpit/`). Talks to Ollama directly; owns todo/Kanban/email/prompt tools + a project hub that links into Open WebUI.

## Stack & conventions (cockpit/)

- Next.js 15 (App Router) + TypeScript (strict). Tailwind 3 + **shadcn/ui** (new-york, neutral). Dark mode via **next-themes** (`attribute="class"`); use theme tokens (`bg-background`, `text-muted-foreground`, `border-border`, …), not raw `neutral-*`.
- Prisma + SQLite. DB access through the singleton in `src/lib/db.ts`. Add models to `prisma/schema.prisma`, then `npm run db:push`.
- **Ollama access goes through `src/lib/ollama.ts`** — `chat()` (one-shot) and `streamChat()` (SSE async generator). Don't scatter raw fetches.
- **AI tools use the shared kit** (keeps every tool consistent and quick to add):
  - Page: a `"use client"` component under `src/app/tools/<name>/` that renders `AiToolShell` (`src/components/tools/AiToolShell.tsx`) with `endpoint`, labels, and a `buildBody` fn.
  - API route under `src/app/api/<name>/`: `runtime="nodejs"`, `dynamic="force-dynamic"`, health-gate with `assertOllamaReady()` (`src/lib/health.ts`), then `return streamTextResponse({ messages, temperature, onComplete })` (`src/lib/ai/streamRoute.ts`). Persist in `onComplete` (runs after the full stream is assembled).
  - Client streaming/state is owned by `useAiTool` (`src/hooks/useAiTool.ts`); the in-band error marker is `ERROR_SENTINEL` (`src/lib/ai/sentinel.ts`).
- **Config & health:** effective config (model / baseUrl / temperature) comes from `getEffectiveConfig()` (`src/lib/config.ts`) = Settings row → env → defaults. The `/settings` page edits the single-row `Settings` model. `HealthBanner` + `/api/health` surface "Ollama down" / "model not pulled".
- **Project links:** new content models take an optional `projectId` (relation to `Project`, `onDelete: SetNull`). The full project hub UI comes later.
- Env vars: `OLLAMA_BASE_URL`, `OLLAMA_MODEL`, `DATABASE_URL`. Local dev reads `cockpit/.env` (gitignored).

## Run / dev

- Full stack (Docker): `./swiss up` → cockpit at :3000, Open WebUI at :3001 (pulls the models first). `./swiss down|status|doctor` stop/inspect/diagnose; `doctor` verifies the Ollama APP-vs-formula rule (llama-server probe), models, Docker, surfaces, and optional voice deps. `./start.sh` is a legacy wrapper for `./swiss up`.
- Local cockpit dev: `cd cockpit && npm install && npm run db:push && npm run dev`.
- Quality gates: `npm run lint` (ESLint flat config), `npm run test:unit` (Vitest — deterministic libs: cosine/jaccard/lintGherkin/normalizeCategory/parseVerdict/…, scoped to `src/**/*.test.ts`), and `npm run test:e2e` (Playwright). The e2e are model-independent (route-mocked), so they pass without Ollama; the unit tests are the real regression net for the deterministic gates.
- The app degrades gracefully when Ollama is absent (health banner + 503 with guidance), so most of it can be built and tested without the model running.

## Status & roadmap

- ✅ Phase 0: engine. ✅ Phase 1: cockpit skeleton + Prompt Optimizer.
- ✅ Foundation: model-tag fix, git, shadcn + dark mode, streaming AI-tool kit, settings + health, ESLint + Playwright.
- ✅ Phase 2: shared template engine + seed (`prisma/seed.mjs`, `npm run db:seed`); Prompt Library (CRUD, search, favorite, variable templates, export/import); Email Writer; Brainstorming (technique modes). Open WebUI prompt sync still deferred until OWUI runs.
- ✅ Phase 3: tasks — one `Task` model as a list + dnd-kit Kanban board; AI assists (goal→tasks, daily standup).
- ✅ Phase 4: memory facts (injected via `lib/memory.ts`) + image input (Gemma vision, `api/vision`). Knowledge base / RAG + PDF live in Open WebUI, deep-linked per project from the hub (`Project.owuiUrl`).
- ✅ Phase 5: project hub + active-project switcher (cookie via `lib/project.ts`, threaded into create routes); quick-capture (`api/capture` token + macOS Shortcut); Open WebUI integration (RAG deep-link + key-configurable prompt sync `api/prompts/sync`).
- ✅ Audit fixes (2026-06-05): (1) task priority/due/edit UI + `EditTaskDialog`; (2) custom template CRUD (`api/templates`, `api/templates/[id]`) + edit ideas/email (`RecentItems` edit dialog, PATCH on `api/ideas|email/[id]`); (3) image/screenshot capture → Idea w/ vision (`Idea.imagePath`, `lib/vision.ts`, `uploads/`); (4) OWUI sync update-or-create + loud failure. Plus the **light Gemma tier** (`gemma4:e4b`) + Settings model picker (`api/models`, `lib/models.ts`). 21/21 e2e, lint + build green.
- ✅ OWUI prompt sync verified end-to-end (2026-06-05). Gotchas baked into `docker-compose.yml`: OWUI needs `ENABLE_API_KEYS=True` (default False, plural) or the API-key UI/endpoints are off. The current OWUI prompt API uses `name` (not `title`) and updates **by id** (`POST /api/v1/prompts/id/{id}/update`) — the old `/command/{cmd}/update` routes are gone; `api/prompts/sync` matches this. Auto-login with `WEBUI_AUTH=False` only holds for browsers with a saved token; a fresh session hits `/auth`.
- ✅ OWUI RAG verified end-to-end (2026-06-05): OWUI embedding set to `ollama`/`embeddinggemma` (via `POST /api/v1/retrieval/embedding/update`); a doc uploaded to a knowledge base was retrieved and answered (made-up codeword test) by local `gemma4:e4b` — fully local. OWUI chat with the local models also confirmed.
- ✅ LBMH project pack (2026-06-05): generalized seeding into re-runnable, idempotent **project packs**. Added nullable unique `sourceKey` (MemoryFact/Prompt/Task) + `Task.module`; `prisma/seed-lbmh.mjs` (`npm run seed:lbmh`) upserts a local pack into facts/templates/prompts/tasks for one project, and skips cleanly when no pack is present. First pack = "LBMH / Spruce QA": 37 Spruce-glossary + BDD/AI-first-standards memory facts, 4 QA templates (Gherkin authoring, eval rubric, AI verification, stress-test), 3 library prompts, 222 training-tracker tasks (xlsx → `Task` with module + status map), and a local OWUI RAG knowledge base (`Project.owuiUrl`, 9 docs over `embeddinggemma`). New committed tool: **Gherkin lint** (`/tools/gherkin-lint`, `lib/gherkinLint.ts`, `api/gherkin-lint`) — the mechanical QA gate (tags, one event, no leakage, `{Type} [name]`). Pack content lives under gitignored `cockpit/projects/` (ECI IP, local-only); the shareable repo keeps the generic seed. Proof: injecting the real facts takes local `gemma4:e4b` Gherkin from BLOCK to PASS on the gate. lint + build green, 27/27 e2e.
- ✅ QA pipeline (2026-06-05): project-scoped **Story → Gherkin → lint → rubric** tool (`/tools/qa-pipeline`, `components/qa/*`, `api/qa-pipeline*`, `lib/qaPipeline.ts`). One-shot JSON (no SSE). `loadProjectQaContext(projectId)` resolves the active project's `lbmh-gherkin-authoring` + `lbmh-qa-eval-rubric` templates **by slug** (never hardcodes glossary) and checks for glossary facts; the orchestrator drafts via `chat()` with the project's memory injected (`getMemoryContext`) + the Gherkin template, lints deterministically by **reusing** `lintGherkin()`, and scores via `chat()` + the rubric template. A project with no pack returns `{ needsPack: true }` (a 200, not a degraded run) → empty-state card pointing to `npm run seed:lbmh`. Verified live on LBMH: a 2-line POS story drafts a tagged `.feature` (one `When` each) using Spruce vocab (POS/tax-exempt/UOM) from the injected glossary, lint is actionable, rubric returns a verdict.
- ✅ QA pipeline — sessions + iteration (2026-06-05): evolved the flat run into a saved, iterable workflow. Replaced `QaRun` with **`QaSession`** (the story) → ordered **`QaIteration`** children (each = draft + lint + rubric, with a follow-up `instruction` and an `edited` flag; `onDelete: Cascade`). Two refine modes: a **model follow-up** (`POST /api/qa-pipeline/[sessionId]` revises the latest draft against "what's missing") and a **manual draft edit** (`PATCH /api/qa-pipeline/iteration/[id]` saves a hand-edit, re-lints instantly/free, and clears the rubric to STALE via `Prisma.DbNull`; `{rescore:true}` re-runs the rubric). Full CRUD: `GET/POST /api/qa-pipeline` (list / new session), `GET/POST/PATCH/DELETE /api/qa-pipeline/[sessionId]` (read / append-iteration / rename / delete), `PATCH/DELETE /api/qa-pipeline/iteration/[id]`. UI is a history list ↔ session view with an iteration timeline (`QaPipeline`/`QaSessionView`/`QaIterationCard`). Lint is recomputed on read (deterministic) so we never store the issue list. Verified live on LBMH (create → list → manual edit clears score → refine → cascade delete) + 9 route-mocked e2e. lint + build green, 36/36 e2e.
- ✅ Memory loop (2026-06-08): turned passive "top 50 facts" injection into a human-in-the-loop, self-evolving loop with two tiers — by model (light extract/classify, embeddinggemma dedupe/rank, `gemma4:12b-mlx` consolidate) and by lifecycle (pending → active → archived). `MemoryFact` gained `category`, `embedding` (JSON `number[]`), `useCount`/`lastUsedAt` (decay signal), and a `mergedInto` self-relation (consolidation lineage). New primitives: `lib/embeddings.ts` (native `/api/embed` via `ollama.embed`, cosine, embeddinggemma query/document prefixes — brute-force in JS, no vector DB), `lib/memoryLoop.ts` (`learnFromText` = capture→classify→dedupe→consolidate; `reindexFacts`; `applyMerge`; `archiveStaleFacts`). `getMemoryContext({projectId, query?})` now relevance-ranks active facts by cosine (pinned always kept, recency fallback) and is threaded the task as `query` from email/templates-run/tasks-generate/qaPipeline; `rankFacts` is read-only. Routes: `POST /api/memory/learn` (replaces `suggest`), `POST /api/memory/reindex`, `GET /api/memory/context` (the relevance inspector), and `PATCH /api/memory/[id]` applies a merge on accept (the only path that edits an active fact, human-gated). UI (`MemoryManager`): category badges, a relevance preview scoped to the active project, merge-proposal review, inline edit, Reindex, and an Archived section. **Hard rules**: long-term memory only changes on human accept (no silent self-edit); deterministic gates wrap each model step (fixed taxonomy, a cosine dedupe cutoff, the human accept). Dedupe cutoff `0.72` calibrated on real embeddinggemma vectors (true paraphrase ~0.79, nearest non-dup ~0.46). Decay auto-archives only model-suggested, unpinned, un-seeded (`sourceKey=null`), never-used facts older than 30 days (reversible). Verified live on LBMH: reindex 37/37; a relevant task ranks the UOM fact at 35% vs 11% for an unrelated one; a paraphrase of the Spruce fact was caught (sim 0.79) and 12B consolidated it (keeping the "RockSolid MAX" detail the note omitted) → accept folded it in with no duplicate. lint + build green, 39/39 e2e.
- ✅ Memory loop — finalization blades (2026-06-08): rounded out the Memory surface into note-taker + organizer + categorizer + lookup. (#3) Fact/glossary search: a client-side search box (key+value) + category filter; the 37-term LBMH glossary is instantly searchable. (#2) Category grouping: the active list groups by category with per-group counts; `POST /api/memory/classify` (`classifyUncategorized`, light model, additive, taxonomy-validated) backfills categories on facts that have none — classified all 37 LBMH facts sensibly (15 glossary / 7 standard / 7 workflow / 3 constraint / 2 project / 2 general / 1 preference). (#5) Opt-in **Learn from activity** (`POST /api/memory/learn-activity`, `learnFromActivity`): runs the loop over the active project's recent ideas / QA stories / task notes (bounded) instead of a pasted note — verified live, scanned 16 sources → 8 durable, categorized QA facts, no dups. All capture stays pending until a human accepts. lint + build green, 39/39 e2e. Three commits on `feat/memory-loop`.
- ✅ Daily enhancements batch (2026-06-09, branch `feat/daily-enhancements`): a sub-agent brainstorm (8 agents → 22 ranked items + frank critic) drove a round of daily/proactive/2027-forward features, each verified live + gated (44/44 e2e). (1) **Command palette** (`⌘K`): `CommandPalette` mounted in layout + `GET /api/search` cross-entity search (prompts/ideas/tasks/facts/email/QA) + sidebar `CommandHint`; `ui/dialog.tsx` gained optional `showClose`. (2) **Today panel** (`DailyBrief`): deterministic proactive home — overdue/due/in-progress tasks + pending memory reviews for the active project, no model call. (3) **`chatJson<T>(messages, schema, opts)`** in `ollama.ts` (Ollama native `/api/chat` `format`=JSON-schema) — the keystone reliability primitive; migrated the memory loop's `extractAndClassify`/`classifyUncategorized` off regex (free-text fallback kept). (4) **NL quick-add** in the palette → `POST /api/quick-add` (chatJson routes a typed note to task/fact/idea, Undo toast). (5) **Import from ticket** on QA → `POST /api/qa-pipeline/ingest` (raw Jira/GitHub issue → clean `As a…/I want…/so that…` story + acceptance criteria, review-before-run). (6) **Elapsed-time** feedback in `useAiTool`/`AiToolShell` ("Running… 12s") for cold 12B loads. (7) **"+ Negative paths"** one-click on a QA session (preset refine appending invalid/security/boundary scenarios, re-linted). (8) **Coverage/gaps panel** on QA iterations — `analyzeCoverage()` in `gherkinLint` (deterministic: scenario count, intent mix, "no negative paths"/"no assertion" gaps), rendered per iteration; pairs with #7. (9) **File export** — `downloadText()` powers an "Export" (→ .md) button on every `AiOutput` and "Export .feature" on QA drafts. (10) **⌘/Ctrl-Enter to run** in `AiToolShell` + the QA story box. (11) **Confirm-before-delete** dialog (`ConfirmDialog`) on the cascade deletes (project, QA session). (12) **"Engine reconnected"** health toast + accurate copy. (13) **Today shows the actual pending facts** (not just a count). (14) **Reliable rubric verdict + 0-100 score** — `extractVerdict()` reads a schema-locked `{verdict, score}` FROM the template's rubric text via `chatJson` (non-destructive; the pack format is untouched); shown as "rubric BLOCK · 75/100". (15) **Soft-delete + Trash for `MemoryFact`** — `deletedAt` + restore/purge; `deletedAt:null` added to all 12 read sites (injection/search/dashboard/dedupe/classify/reindex/decay/glossary/GET), verified live that a deleted fact leaves every surface incl. the injection path. **After pulling, run `npm run db:push`.** Then, on request, the rest of the roadmap was built too (the critic's cuts included): (16) **"Context used" provenance** panel on injecting tools (`ContextUsed` + `/api/memory/context` defaults to active project). (17) **Per-tool model** override (`Settings.qaModel`; QA can run on 12b-mlx for rigor). (18) **Activity timeline** (`ActivityLog` + `logActivity` + `/tools/activity`). (19) **Routine runner** (`/api/routines/[slug]` token-authed: `standup`/`wrapup` → dated Idea). (20) **Eval bench** (`GoldenCase` + `/golden` + `/bench`, agreement %). (21) **Voice capture** (`/api/transcribe` whisper.cpp + mic in palette; graceful 503 when whisper/ffmpeg absent — `brew install whisper-cpp ffmpeg` to enable). (22) **Bug report writer** (`/tools/bug-report`, `BugReport`, chatJson + completeness gate). (23) **Global export/import** (`/api/export` + `/api/import`, idempotent; Settings backup UI). (24) **"Ask anything"** one-shot Q&A in the palette (`/api/ask`). (25) **Smart Inbox** (`/tools/inbox`, drop/paste → quick-add or Gherkin Lint). **Perf lesson:** never inject the full memory context into a `chatJson` (structured) call on the light model — a 37-fact system block made bug-report take ~3min; dropping it → ~10s. (memory injection belongs on the streaming drafting tools, not structured extraction.) Three new schema models (BugReport, GoldenCase, ActivityLog) + `MemoryFact.deletedAt`/`Settings.qaModel`/`qaModel` — **run `npm run db:push` after pulling.** Branch `feat/daily-enhancements` pushed to GitHub (26 features, ~30 commits) and since **merged into `main`** (the hardening audit + everything after sits on top of it).
- ✅ Pre-production hardening audit (2026-06-09, branch `main`): an adversarial 17-agent audit (8 dimensions × find→verify + completeness critic) surfaced 40 verified findings; the proven criticals/highs were fixed in 8 gated commits (each: stop dev → `rm -rf .next` → build → lint → unit → e2e → one commit), every fix verified live. **Security (`fix(security)`):** `PUT /api/settings` accepted any `baseUrl` (SSRF + silent off-machine prompt routing — breaks the local-only rule); now allowlisted to loopback/`host.docker.internal` (repro `http://169.254.169.254/v1` → 400). The capture/routine token is **header-only** now (`x-capture-token`, the documented recipe; query-param dropped — it leaked to logs) and compared with `crypto.timingSafeEqual` (`lib/captureAuth.ts`); capture image capped (413 > ~15 MB). **Voice (`fix(voice)`):** transcribe temp path was `sk-voice-${pid}-${buf.length}` → two same-size concurrent uploads clobbered each other; now `randomUUID()`. ffmpeg/whisper spawns get a 60s SIGKILL timeout (→504); audio capped at 25 MB. **Backups (`fix(data)`):** `/api/export` leaked soft-deleted (Trash) facts (no `deletedAt:null`) — fixed and verified (a trashed fact is absent from the export). Import/`prompts/import` no longer report dropped rows as success — `upsertAll` returns `{ok,failed}`, the response carries a `skipped` map (DataBackup warns), and malformed (non-array) fields no longer 500. **Resilience (`fix(resilience)`):** added the missing React error boundaries — `app/error.tsx`, `app/global-error.tsx`, `app/not-found.tsx`, `app/tools/loading.tsx` (a thrown component used to white-screen the whole app); all six Ollama fetches now carry an env-tunable timeout (gen 300s / embed 60s via `AbortSignal.any`) so a hung-but-up engine can't pin a request; `streamTextResponse` aborts upstream on client disconnect (stream `cancel()`) and persists in a separate try so a post-stream save failure reads as a save note (draft kept), not a generation error. **Reliability (`fix(reliability)`):** board reorder is now an atomic `$transaction` that 500s on failure (was fire-and-forget `.catch(()=>null)` reported as `{ok:true}`; the client surfaces it); QA session/iteration DELETE only treat P2025 as idempotent-ok (other errors → 500/404, matching the sibling routes); the eval bench is partial-safe (one failing case records `ERROR`, doesn't 500 the run). **Correctness (`fix(correctness)`):** invalid `dueDate` strings → 400 (were a 500 on create / misleading 404 on patch); deleting the active project clears the dangling `activeProjectId` cookie; the tasks page normalizes (not blind-casts) the DB status/priority enums. **Tests (`test(unit)`):** added **Vitest** (dev-only) + **52 unit tests** for the deterministic safety-net libs, and the tests caught two real bugs: `cosine` silently scored the common prefix on a length mismatch (now → 0) and `parseVector` only checked `v[0]` (a mixed array poisoned cosine with NaN; now every element must be finite). **Perf:** `rankFacts` findMany gained `take:200` (bounds the brute-force cosine scan if the corpus grows; no-op at 37 facts). All gates green throughout: build, lint, **52/52 unit, 44/44 e2e**; LBMH dev DB untouched (37 active / 0 pending verified after each batch). Remaining LOW items accepted for a single-user local app (the `_max.order+1` create race; the unauthenticated `GET /api/capture/token` — inherent to a localhost tool, mitigate by binding 127.0.0.1 before LAN sharing; 2 moderate `npm audit` advisories — don't `audit fix --force`, it downgrades next).
- ✅ Dev + AI-engineer tools round (2026-06-09, `main`, Fable 5 + ULTRACODE): six new tools for the developers/AI engineers on the team, each = kit-based page + **deterministic gate wrapping the model step** + sidebar/palette entries + unit tests for the lib + route-mocked e2e + live verification on real Gemma, one gated commit each. (1) **ADR Writer** (`/tools/adr`, `lib/adrLint.ts`, `Adr` model — **run `npm run db:push` after pulling**): note → streamed MADR draft (memory-injected) → lint gate (missing/empty required sections, <2 considered options, zero negative consequences = ERROR; fence-aware so code samples can't fake headings) → save with status lifecycle + .md export; CRUD at `api/adr` (P2025-idempotent delete). (2) **Code Review** (`/tools/code-review`, `lib/codeSmells.ts`): dependency-free scanner — strings/comments/regex literals stripped via a state machine (line numbers preserved), per-function cyclomatic/nesting/params (nested fns excluded from parent metrics), magic numbers (SCREAMING_CASE consts exempt), dup windows; accepts raw code OR a unified diff (added lines only, new-file line numbers, `\ No newline` markers handled); the model only EXPLAINS the findings (streamed, **no memory injection** — third-party code + facts = prompt-injection disclosure path), and a clean scan skips the model. (3) **Eval Rubric Designer** (`/tools/rubric-designer`, `lib/rubric.ts`): bar → chatJson `{criteria+weights, bands, PASS/BLOCK samples}` → gate (weights sum to exactly 100 with largest-remainder auto-repair, bands tile 0–100 monotonically) → **live separation check through the same `scoreFeature`/`extractVerdict` path the QA pipeline uses** → saved as a Template under `qa-eval-rubric:<projectId>`, which `loadProjectQaContext` resolves BEFORE the LBMH pack slug (designed rubrics override; bench gate loosened to rubric-only). (4) **Eval Case Generator** (`/tools/eval-cases`, `lib/evalCases.ts`): spec → cases across happy/boundary/adversarial/ambiguous/out-of-scope; gate = ≥1 per dimension + verdict sanity; embeddinggemma dedupe at the calibrated 0.72 (pure `markDuplicates`, embed failure degrades to "skipped"); each accept writes one existing-`GoldenCase` row (human-gated) → feeds the bench. (5) **API Contract Designer** (`/tools/api-contract`, `lib/openapiLint.ts` + deps `@seriousme/openapi-schema-validator` + `yaml` + direct `ajv@8` pin to fix legacy-peer-deps hoisting): prose → chatJson loose envelope → REAL offline OpenAPI 3.1 meta-schema validation + design rules (no 2xx/4xx = ERROR, no 5xx/operationId = WARN, unpaginated collection GET = WARN); pasting an `openapi:` doc is auto-detected and linted with zero model calls; .yaml export. (6) **Complexity Analyzer** (`/tools/complexity`, `lib/complexity.ts`): chatJson `{timeBigO, spaceBigO, hotspots}` (no memory) audited by a growth-mechanism scan (loop nesting incl. iteration callbacks, recursion, sort) — super-linear claims with no mechanism → WARN ("a static scan can't prove Big-O, it can prove absent mechanisms"); a second streamed route derives the bound from the scan facts. Mid-round, a 25-agent adversarial review (find→refute) confirmed 9 issues — all fixed + regression-tested in `fix:` 00f8873 (regex-at-statement-start lexing, diff `\ No newline` truncation, fenced-heading false BLOCKs, malformed-spec 500s, ADR size caps, memory-injection removal from code review, e2e mock copy drift).
- ✅ Onboarding + UX round (2026-06-09, `main`): (a) **`./swiss up|down|status|doctor`** — one CLI for the stack; `doctor` encodes the APP-cask-vs-brew-formula rule (llama-server probe + formula detection with the uninstall/cask fix), engine/models/Docker/surfaces/voice checks with copy-paste fixes; README's `brew install ollama` trap corrected (cask + why), `start.sh` is now a wrapper, in-app engine-down copy points at the app. (b) **First-run UX**: time-of-day greeting + optional `Settings.userName` (**db:push**) editable in Settings; an empty DB shows a first-run card (local-only + `./swiss doctor`, projects optional / no-project = global, ⌘K); the project switcher reads "No project — global" with a hover explanation. (c) **Shared UI states**: `EmptyState`/`LoadingState`/`ErrorAlert` components adopted across the new tools + QA list + segment loader; the badge-cutoff class killed at the source (`whitespace-nowrap` in badgeVariants, `shrink-0` on the QA badge group, `min-w-0`+truncate on session rows); **responsive sidebar** — below `md` the aside collapses to a hamburger top bar + drawer of the same server-rendered content (overlay/Esc/link-click closes), with a mobile/desktop e2e.
- ✅ DX round (2026-06-09, `main`): (1) **`usePersisted`** (`hooks/usePersisted.ts`, useSyncExternalStore, SSR-safe) extracted from SidebarNav and adopted for Tasks (priority/module filters + board↔list tab survive reloads, stale stored values degrade to "all") and Memory (project/category filters); search boxes stay ephemeral. (2) **Clickable filter chips**: task module/priority badges (board cards via optional callbacks threaded to TaskCard, and list rows) and Memory's CategoryBadge/group headers/project badges set the matching filter on click. (3) **Palette actions + ⌘P**: ⌘K actions (toggle theme, reindex memory, new QA, standup/wrap-up — the palette fetches the capture token and calls the token-authed routine route) and ⌘P/Ctrl+P project mode (list + filter + switch via the active-project cookie endpoint, also a "Switch project…" action); palette/nav drift fixed (Smart Inbox, Bug Report, Activity now jumpable). (4) **Bulk multi-select**: Tasks list (status/priority/delete-behind-ConfirmDialog over per-row endpoints, optimistic + surfaced failures) and Memory active facts (archive / move-to-trash, ONE refresh per batch); bulk-accepting pending/merge memory is deliberately NOT offered — accepts stay per-fact human decisions. All verified live; suite at this point: **154 unit / 79 e2e**, every commit individually gated.
- **Shipped / next (2026-06-09 round close):** shipped = 6 dev/AI-engineer tools (ADR, Code Review, Rubric Designer, Eval Cases, API Contract, Complexity — every model step wrapped in a deterministic gate), `./swiss` ops CLI + corrected onboarding (cask rule), first-run UX + responsive sidebar + shared UI states, the DX round above, and two adversarial review passes with all confirmed findings fixed. Queued/next: macOS capture Shortcut (below), Open WebUI prompt-sync UX polish, per-tool model overrides beyond QA, and a virtualization/useMemo pass if task/fact counts grow past ~1k.
- ✅ Production-ready round (2026-06-10, branch `feat/production-ready`, Fable 5 + Codex `gpt-5.5` as debate pair): a 25-agent per-feature audit (every tool judged as a daily SWE+SDET+AI-engineer instrument, file:line cited) + live Playwright-MCP walkthrough + two Codex debate rounds produced a locked plan; ~60% of suggested improvements were deliberately deferred to keep the app SIMPLER. Nine gated commits (each: clean build with `env -u NODE_ENV` — a shell `NODE_ENV=development` poisons `next build`'s error-page prerender with a fake `<Html>` import error — + lint + unit + e2e). **Portfolio decisions (debated to consensus):** Complexity Analyzer CUT as a destination and folded into Code Review as an opt-in "Estimate Big-O" pass (`BigOSection`; the derivation never auto-runs) — the smell scan gained free deterministic growth WARNs (`withGrowthWarnings`: 3-deep nesting → "likely O(n³)"); Image and Email Writer KEPT after debate (intent-split + privacy-moat arguments beat Codex's round-1 cuts); navigation rebuilt around ONE registry (`lib/nav.tsx` gains `group`+`keywords`; the palette derives from it — the hand-copied list had drifted twice; sidebar gets Work/Write/QA & Evals/Dev/System headers; dashboard grid is favorites-first; `nav.test.ts` trips on page↔registry drift). **Trust pattern killed everywhere:** every "X & save" used to RE-RUN the model at save time — optimizer/email/template-runner/bug-report now save the reviewed result verbatim (new `POST /api/prompts`, `POST /api/ideas`, `persist` branches; email titles from the draft's `Subject:` line; bug-report gate extracted to `lib/bugReport.ts`). **Bugs fixed (worst first per Codex ranking):** memory-loop integrity — PATCH value re-embeds (stale-meaning ranking forever), every create path embeds best-effort, quick-add facts (model paraphrases) now land `pending` like all model writes; board drags re-stamped `completedAt` on ALL done tasks (broke wrapup); due dates are calendar days (`lib/dates.ts`, UTC-noon storage, ISO-day display — kills the west-of-UTC off-by-one); QA rubric-score failure no longer discards a multi-minute draft (persists `score=null` → STALE); React #418 on every page (CommandHint's server "Ctrl K" vs Mac "⌘K" — `useIsMac` via useSyncExternalStore); DailyBrief showed a green all-clear on DB failure; `/api/export` silently dropped ADRs (+ `backupCoverage.test.ts` tripwire); health gates now probe the model each route ACTUALLY runs (visionModel/qaModel); `useAiTool` aborts on unmount (engine stopped generating for closed views). **Power-user flow:** ⌘K search results deep-link to the item (`?session=`/`?q=`/`?ideaId=` consumed by QA/tasks/memory/brainstorm); Inbox lints Gherkin INLINE + prefills Gherkin Lint via sessionStorage (the old clipboard bounce landed on an empty page); Image gets paste (⌘V)/drag-drop/Save-as-Idea (shared `lib/uploads.ts`); quick-add parses date phrases deterministically (`lib/quickDates.ts` — "tomorrow"/"on friday"/"in 3 days" become real due dates) with a loading toast; tasks page project-scopes with a persisted All-projects toggle; standup is one shared scoped+bounded builder; golden cases became human-labeled (PASS/BLOCK choice at save, manager UI with relabel/delete, per-case bench results — engine ERRORs distinct from drift). Closed with a fresh-context Codex adversarial review of the whole branch (5 must-fix confirmed and fixed in the final commit: save-after-run snapshots the inputs that produced the output; goldens attach to the SESSION's project; `tasks/[id]` PATCH is transition-only like reorder; Big-O derivation runs on the analyzed snapshot; prompt/email ⌘K deep links) and a final convergence sign-off on the debate thread. Suite: **246 unit / 81 e2e**, all green.
- ✅ Windows support (2026-06-10, branch `feat/production-ready`): an 8-dimension Windows-compat audit (52 findings) drove the port. **New:** `swiss.ps1` (PowerShell 5.1+7 port of up/down/status/doctor — Ollama-for-Windows install checks, GPU/RAM notes, MLX-tag trap detection, winget/whisper-release fix-its) + `swiss.cmd` (`.\swiss up` works in cmd/PS with no execution-policy fuss) + `scripts/pull-models.ps1` + `scripts/sk-capture.ps1` (Get-Clipboard quick capture; hotkey recipe in Settings → Quick capture's new Windows section). **Blockers fixed:** `.gitattributes` (CRLF checkouts broke `docker-entrypoint.sh` in the container — plus a `sed` belt-and-suspenders in the Dockerfile); pull scripts are platform-aware (quality = `gemma4:12b` GGUF off-Apple; light tier pulls FIRST and quality soft-fails so no machine strands with zero models); `models.ts` gained the GGUF quality preset and labels MLX as Apple-Silicon-only. **Degraded→fixed:** `memoryLoop` quality model env-overridable (`OLLAMA_QUALITY_MODEL`); transcribe 503s give winget/whisper-release hints on win32 (voice is local-dev-only by design — the container has no ffmpeg) + EINVAL (.cmd shim) handled; engine-down copy platform-neutral (health.ts, HealthBanner — deliberately NOT process.platform-branched: the cockpit runs in a Linux container); SettingsForm/compose/README/first-run copy de-Mac'd; Image-page paste hint uses useIsMac. **Container hygiene found by the audit:** `cockpit-uploads` volume (captured images previously vanished on rebuild), `.dockerignore` extended (`.env` was baked into image layers), Dockerfile `npm ci` + `.npmrc` copy, `engines: node>=22`, cross-platform `npm run clean`. README gained Windows prerequisites/run/troubleshooting (winnat port reservations, re-normalize line endings, execution policy). Validation: pwsh 7.4.6 parse + live status/doctor runs on macOS, a full fresh-clone `./swiss up` rehearsal on macOS (first-run card + live model call through the new stack), and **verified on real Windows hardware by Ozzy (2026-06-10): `.\swiss doctor` all green.** Merged to `main` the same day.
- ✅ Production-readiness audit (2026-06-10, Fable 5 + ULTRACODE): a 61-agent adversarial review of the two rounds above (8 dimensions, find → refute-verify → completeness critic: 42 confirmed + 6 critic finds, 0 refuted) + a live Playwright-MCP persona walkthrough on a true fresh clone (no .env/DB/node_modules — the colleague day-1 path, on real Gemma) + EXECUTED deployment checks (built image inspected: dev.db absent, TZ reaches Node; swiss.ps1 run inside the official pwsh 7.4 arm64 Docker image, including the daemon-down crash state). 21 findings fixed in-tree, all re-gated and live-verified. Highest first: **swiss.ps1 crashed under WinPS 5.1 whenever Docker Desktop was stopped** (native stderr + `2>$null` under EAP=Stop throws — the exact state doctor diagnoses; fixed with function-local EAP + try/catch); `.gitattributes` **did not exist** (the CRLF "fix" above was phantom — created); `.dockerignore` `*.db` was root-anchored so `prisma/dev.db` (capture token + facts) was **baked into the image** (now `**/*.db`, verified absent from a rebuild); container had no TZ so all calendar-day math ran in UTC (compose `TZ=${TZ:-}` + both CLIs export the host zone — verified inside the container); wrapup queries were unscoped but stamped the active project (now scoped like standup); ⌘K deep links were masked by persisted filters/scope and were no-ops same-page (now lift filters + sync on prop change); Inbox lint went stale on edit; eval verdict overrides survived regenerate (silently mislabeling goldens); memory PATCH gained guards (empty value → 400, merge-accept respects Trash, "accepted" logs only on a real pending→active); golden PATCH only 404s on P2025; ideas API 400s unparseable images; DailyBrief due-today can't be starved (date-bounded desc query); diff-mode growth WARNs require ADDED iteration; Big-O analyzes the new-file side of diffs at new-file lines; the foreign-language `end` tell no longer fires on TS `end:` properties (this repo has one). +2 regression tests (251 unit / 81 e2e, lint + build green). Remaining low/product-decision items: `PRODUCTION-READINESS-2026-06-10.md`. History note: the production-ready round, Windows support, and this audit landed on `main` as ONE squashed commit — the per-feature branch commits described above were folded into it.
- ⏳ Only remaining (pre-existing): build the macOS capture Shortcut from the documented recipe (Settings → Quick capture). See `HANDOFF-CONTINUE.md`.

## Working agreements for the agent

- When writing code against a library/framework, **use Context7** ("use context7") to pull current, version-accurate docs rather than relying on training data.
- For end-to-end UI testing, **use the Playwright MCP** explicitly, or run `npm run test:e2e`.
- Keep changes small and committed per feature so they're easy to roll back.
- Update this file and the roadmap when a phase lands.
